# aws-iam-checker

AWS IAM role and policy checker CLI.

## For AI agents
- scripts/check.py lists IAM roles via AWS CLI
- Use --json for structured output
- Detects and flags unused roles
- Requires AWS CLI installed and configured
